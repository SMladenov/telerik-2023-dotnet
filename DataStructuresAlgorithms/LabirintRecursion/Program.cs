using System;
using System.Data;

namespace LabirintRecursion
{
    internal class Program
    {
        static void Main(string[] args)
        {
            FindPath(0, 0);
        }

        static void FindPath(int row, int col)
        {
            // check if inside lab
            
            // check if we hit a wall

            // check if we already visited

            // check if we are at the exit, print path

            // step on cell

            // the magic of recursion
        }


    }
}
